# 1.Hoisting

    . một hành vi mặc định của Javascript
    . đưa phần khai báo lên đầu phạm vi
    . áp dụng với : function(Declaration function), var, let, const
    . let, const : hoisting => đưa vào Temporal dead zone(TDZ) (vùng không sử dụng được)
